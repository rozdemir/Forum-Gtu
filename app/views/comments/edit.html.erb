<% if current_user && current_user==@comment.user%>

<h1>Editing Comment</h1>

<%= render 'form' %>

<%= link_to 'Show', "/posts/#{@comment.post_id}" %> |
<%= link_to 'Back', "/posts/#{@comment.post_id}" %>
<% end %>